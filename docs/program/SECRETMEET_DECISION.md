# SecretMeet keep vs sunset

## Decision (executed)

**Keep** SecretMeet with moderation controls:

- Queue rate limits  
- `POST /api/secret/report`  
- `POST /api/secret/block` + match filtering  
- Clearer in-product moderation copy  

Revisit sunset only if moderation load exceeds ~1 eng-week/quarter or App Store / ToS risk rises.

## Owners

Meetings TL + Growth EM.
